{
  "name": "@pulsar-http/core",
  "version": "0.1.1",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "build": "bun build src --outdir ./dist --target bun --minify --root src && tsc  --emitDeclarationOnly"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "tsconfig-paths": "^4.2.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pulsar-http/core.git"
  }
}